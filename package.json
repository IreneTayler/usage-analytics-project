{
    "name": "usage-analytics-cache",
    "version": "1.0.0",
    "description": "Usage analytics system with daily aggregates caching",
    "scripts": {
        "dev": "ts-node src/server.ts",
        "build": "tsc",
        "start": "node dist/server.js",
        "migrate": "npx prisma migrate dev",
        "generate": "npx prisma generate",
        "seed": "ts-node src/seed.ts"
    },
    "dependencies": {
        "@prisma/client": "^5.0.0",
        "express": "^4.18.2"
    },
    "devDependencies": {
        "@types/express": "^4.17.17",
        "@types/node": "^20.0.0",
        "prisma": "^5.0.0",
        "ts-node": "^10.9.1",
        "typescript": "^5.0.0"
    }
}